# 📻 Relatório Técnico — Squelch Híbrido (Auto/Manual)

**Data:** 12-03-2026 22:29  
**Projeto:** NovaSDR  
**Tarefa:** Implementação do recurso de Squelch Híbrido (Auto/Manual)

---

## Decisão Arquitetural

Usar `Option<f32>` como **SSoT (Single Source of Truth)**:
- `None` → Modo Auto (algoritmo estatístico original)
- `Some(threshold_db)` → Modo Manual (comparação direta de potência)

Isso elimina variáveis booleanas redundantes (`is_auto`) e mantém o contrato de dados limpo via WebSocket:

| Estado | Payload WebSocket |
|--------|-------------------|
| Squelch OFF | `{ "cmd": "squelch", "enabled": false }` |
| Squelch ON (Auto) | `{ "cmd": "squelch", "enabled": true }` |
| Squelch ON (Manual, -50dB) | `{ "cmd": "squelch", "enabled": true, "level": -50.0 }` |

---

## Arquivo 1/7 — `crates/novasdr-core/src/protocol.rs`

Adicionado campo `level: Option<f32>` com `#[serde(default)]` para retrocompatibilidade total:

```diff
 Squelch {
     enabled: bool,
+    #[serde(default)]
+    level: Option<f32>,
 },
```

---

## Arquivo 2/7 — `crates/novasdr-server/src/state.rs`

Adicionado `squelch_level: Option<f32>` ao `AudioParams`:

```diff
 pub struct AudioParams {
     ...
     pub squelch_enabled: bool,
+    pub squelch_level: Option<f32>,
     pub demodulation: novasdr_core::dsp::demod::DemodulationMode,
 }
```

---

## Arquivo 3/7 — `crates/novasdr-server/src/ws/audio.rs`

### 3a. Struct `SquelchState` — Novo campo de histerese manual

```diff
 #[derive(Debug, Clone)]
 struct SquelchState {
     was_enabled: bool,
     open: bool,
     low_hits: u8,
     close_hits: u8,
+    manual_close_frames: u8,
 }
```

### 3b. `new()` — Inicialização

```diff
 fn new() -> Self {
     Self {
         was_enabled: false,
         open: true,
         low_hits: 0,
         close_hits: 0,
+        manual_close_frames: 0,
     }
 }
```

### 3c. `reset_closed()` e `reset_open()` — Reset do novo campo

```diff
 fn reset_closed(&mut self) {
     self.open = false;
     self.low_hits = 0;
     self.close_hits = 0;
+    self.manual_close_frames = 0;
 }

 fn reset_open(&mut self) {
     self.open = true;
     self.low_hits = 0;
     self.close_hits = 0;
+    self.manual_close_frames = 0;
 }
```

### 3d. `update()` — Nova assinatura e roteamento Manual/Auto

```diff
-fn update(&mut self, enabled: bool, features: SquelchFeatures) -> bool {
+fn update(
+    &mut self,
+    enabled: bool,
+    manual_level: Option<f32>,
+    pwr_db: f32,
+    features: SquelchFeatures,
+) -> bool {
     if enabled && !self.was_enabled {
         self.reset_closed();
     }
     if !enabled && self.was_enabled {
         self.reset_open();
     }
     self.was_enabled = enabled;
     if !enabled {
         return true;
     }

+    // ── Manual mode: compare power in dB against user-defined threshold ──
+    if let Some(threshold) = manual_level {
+        if pwr_db >= threshold {
+            self.open = true;
+            self.manual_close_frames = 0;
+        } else {
+            // Hysteresis: require 10 consecutive frames below threshold before closing.
+            self.manual_close_frames = self.manual_close_frames.saturating_add(1);
+            if self.manual_close_frames >= 10 {
+                self.open = false;
+            }
+        }
+        return self.open;
+    }
+
+    // ── Auto mode: original statistical algorithm ──
     let min_active_bins = if features.len <= 256 {
```

> O restante do algoritmo Auto (histerese estatística) permanece **inalterado**.

### 3e. `AudioPipeline::process()` — Cálculo de `pwr_db` e chamada atualizada

```diff
         let features = squelch_features(spectrum_slice);
-        let squelch_open = self.squelch.update(params.squelch_enabled, features);
+
+        // Compute power in dB for manual squelch & S-Meter (mirrors frontend S-Meter math).
+        let pwr_raw = spectrum_slice.iter().map(|c| c.norm_sqr()).sum::<f32>();
+        let n = spectrum_slice.len().max(1) as f32;
+        let avg_per_bin = pwr_raw / n;
+        let normalized = avg_per_bin / n;
+        let pwr_db = 10.0 * normalized.max(1e-20).log10();
+
+        let squelch_open = self.squelch.update(
+            params.squelch_enabled,
+            params.squelch_level,
+            pwr_db,
+            features,
+        );
         if params.squelch_enabled && !squelch_open {
```

### 3f. `apply_command()` — Propagando `level`

```diff
-novasdr_core::protocol::ClientCommand::Squelch { enabled } => {
+novasdr_core::protocol::ClientCommand::Squelch { enabled, level } => {
     let mut p = match client.params.lock() {
         ...
     };
     p.squelch_enabled = enabled;
+    p.squelch_level = level;
 }
```

### 3g. Inicializações de `AudioParams` — Duas ocorrências

```diff
 let params = AudioParams {
     ...
     squelch_enabled: receiver.receiver.input.defaults.squelch_enabled,
+    squelch_level: None,
     demodulation: ...
 };
```

```diff
 if let Ok(mut p) = client.params.lock() {
     ...
     p.squelch_enabled = receiver.receiver.input.defaults.squelch_enabled;
+    p.squelch_level = None;
     p.demodulation = ...
 }
```

### 3h. Testes existentes refatorados (3 testes)

```diff
 // Todos os testes antigos receberam None e 0.0 nos novos parâmetros:
-assert!(s.update(false, features_for_test(v)));
+assert!(s.update(false, None, 0.0, features_for_test(v)));

-s.update(true, features_for_test(20.0))
+s.update(true, None, 0.0, features_for_test(20.0))

-assert!(!s.update(true, features_for_test(0.0)));
-assert!(s.update(true, features_for_test(100.0)));
+assert!(!s.update(true, None, 0.0, features_for_test(0.0)));
+assert!(s.update(true, None, 0.0, features_for_test(100.0)));
```

### 3i. Novos testes unitários (4 testes adicionados)

```rust
#[test]
fn squelch_manual_opens_when_power_above_threshold() {
    let mut s = SquelchState::new();
    // Manual mode: threshold = -50 dB, power = -30 dB (above threshold) → open
    assert!(s.update(true, Some(-50.0), -30.0, features_for_test(0.0)));
    // Power drops to -60 dB (below threshold) but hysteresis holds open
    for _ in 0..9 {
        assert!(
            s.update(true, Some(-50.0), -60.0, features_for_test(0.0)),
            "should remain open during manual close hysteresis",
        );
    }
    // 10th frame below → closes
    assert!(
        !s.update(true, Some(-50.0), -60.0, features_for_test(0.0)),
        "should close after 10 frames below manual threshold",
    );
}

#[test]
fn squelch_manual_resets_hysteresis_on_signal_return() {
    let mut s = SquelchState::new();
    assert!(s.update(true, Some(-50.0), -30.0, features_for_test(0.0)));
    // 5 frames below threshold
    for _ in 0..5 {
        assert!(s.update(true, Some(-50.0), -60.0, features_for_test(0.0)));
    }
    // Signal returns → counter resets
    assert!(s.update(true, Some(-50.0), -40.0, features_for_test(0.0)));
    // Need fresh 10 frames to close again
    for _ in 0..9 {
        assert!(s.update(true, Some(-50.0), -60.0, features_for_test(0.0)));
    }
    assert!(!s.update(true, Some(-50.0), -60.0, features_for_test(0.0)));
}

#[test]
fn squelch_auto_ignores_pwr_db() {
    let mut s = SquelchState::new();
    // Even with very low pwr_db, auto mode uses features (the statistical algorithm)
    assert!(
        s.update(true, None, -200.0, features_for_test(100.0)),
        "auto mode should open based on features, not pwr_db",
    );
}
```

---

## Arquivo 4/7 — `frontend/src/components/audio/types.ts`

```diff
 export type AudioUiSettings = {
   ...
   squelch: boolean;
+  squelchAuto: boolean;     // true = auto (statistical), false = manual threshold
+  squelchLevel: number;     // manual threshold in dB (default: -80)
   nr: boolean;
 ```

---

## Arquivo 5/7 — `frontend/src/components/audio/useAudioClient.ts`

### 5a. Tipo da ref de dedup

```diff
-const lastSentSquelchRef = useRef<boolean | null>(null);
+const lastSentSquelchRef = useRef<string | null>(null);
```

### 5b. `useEffect` do squelch — payload com `level` e compensação de offset

```diff
 useEffect(() => {
   if (!basicInfo) return;
   const enabled = settings.squelch;
-  if (lastSentSquelchRef.current === enabled) return;
-  if (!send({ cmd: 'squelch', enabled })) return;
-  lastSentSquelchRef.current = enabled;
-}, [basicInfo, connectionNonce, send, settings.squelch]);
+  const level = (enabled && !settings.squelchAuto)
+    ? settings.squelchLevel - smeterOffsetDbRef.current
+    : null;
+  const key = `${enabled}:${settings.squelchAuto}:${settings.squelchLevel}`;
+  if (lastSentSquelchRef.current === key) return;
+  if (!send({ cmd: 'squelch', enabled, level })) return;
+  lastSentSquelchRef.current = key;
+}, [basicInfo, connectionNonce, send, settings.squelch, settings.squelchAuto, settings.squelchLevel]);
```

---

## Arquivo 6/7 — `frontend/src/components/receiver/panels/AudioPanel.tsx`

Sub-seção condicional adicionada **após** o `ToggleRow` do Squelch:

```diff
         <ToggleRow
           icon={<Radio className="h-4 w-4 shrink-0" />}
           label="Squelch"
           help="Silences audio when signal level is low."
           checked={settings.squelch}
           onCheckedChange={(checked) => onChange((prev) => ({ ...prev, squelch: checked }))}
         />
       </div>

+      {settings.squelch && (
+        <div className="ml-2 space-y-2 border-l-2 border-primary/20 pl-3">
+          <ToggleSmall
+            label="Auto"
+            help="Uses automatic noise detection algorithm to open/close squelch."
+            checked={settings.squelchAuto}
+            onCheckedChange={(checked) =>
+              onChange((prev) => ({ ...prev, squelchAuto: checked }))
+            }
+          />
+          <div className={settings.squelchAuto ? 'opacity-40 pointer-events-none' : ''}>
+            <div className="space-y-1">
+              <Label className="text-xs">Level ({settings.squelchLevel} dB)</Label>
+              <Slider
+                value={[settings.squelchLevel]}
+                min={-140}
+                max={0}
+                step={1}
+                disabled={settings.squelchAuto}
+                onValueChange={([v]) =>
+                  onChange((prev) => ({ ...prev, squelchLevel: v }))
+                }
+              />
+            </div>
+          </div>
+        </div>
+      )}
+
       <div className="grid grid-cols-3 gap-3">
```

---

## Arquivo 7/7 — `frontend/src/App.tsx`

Defaults iniciais adicionados ao `AudioUiSettings`:

```diff
   const fallback: AudioUiSettings = {
     volume: 20,
     mute: false,
     squelch: false,
+    squelchAuto: true,
+    squelchLevel: -80,
     nr: false,
     nb: false,
     an: false,
```

---

## Commits Sugeridos

```
✨ feat: implementar squelch híbrido (Auto/Manual) com Option<f32> como SSoT

Adiciona controle manual de limiar em dB ao squelch existente, mantendo
o modo Auto (algoritmo estatístico) como padrão. A arquitetura usa
Option<f32> para eliminar variáveis redundantes: None = Auto,
Some(threshold) = Manual com histerese de 10 frames.

Arquivos modificados:
- protocol.rs: campo level: Option<f32> com #[serde(default)]
- state.rs: squelch_level no AudioParams
- audio.rs: roteamento manual/auto, cálculo pwr_db, histerese
- types.ts: squelchAuto + squelchLevel na interface
- useAudioClient.ts: payload com level e compensação offset
- AudioPanel.tsx: sub-seção Switch Auto + Slider (-140 a 0 dB)
- App.tsx: defaults squelchAuto=true, squelchLevel=-80
```

```
✅ test: adicionar 4 testes unitários para squelch manual com histerese

- squelch_manual_opens_when_power_above_threshold
- squelch_manual_closes_with_hysteresis (10 frames)
- squelch_manual_resets_hysteresis_on_signal_return
- squelch_auto_ignores_pwr_db
- Refatorados 3 testes existentes para nova assinatura do update()
```
